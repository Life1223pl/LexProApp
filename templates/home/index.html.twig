{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <div style="max-width: 900px; margin: 24px auto;">

        <h1>Dashboard</h1>

        <p>Witaj w systemie obsługi postępowań.</p>

        <div style="margin-top: 24px; display: flex; gap: 16px; flex-wrap: wrap;">
            <a href="{{ path('app_postepowanie_index') }}"
               style="
               padding: 14px 18px;
               border: 1px solid #333;
               border-radius: 6px;
               text-decoration: none;
               font-weight: bold;
           ">
                📂 Lista postępowań
            </a>

            <a href="{{ path('app_postepowanie_new') }}"
               style="
               padding: 14px 18px;
               border: 1px solid #333;
               border-radius: 6px;
               text-decoration: none;
           ">
                ➕ Nowe postępowanie
            </a>

            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('admin_pracownicy_index') }}"
                   style="
                   padding: 14px 18px;
                   border: 1px solid #333;
                   border-radius: 6px;
                   text-decoration: none;
               ">
                    👥 Panel administratora
                </a>
            {% endif %}
        </div>

    </div>
{% endblock %}
