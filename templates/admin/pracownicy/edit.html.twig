{% extends 'base.html.twig' %}

{% block title %}Edytuj pracownika{% endblock %}

{% block body %}
    <div style="max-width: 900px; margin: 24px auto;">
        <h1>Edytuj pracownika #{{ pracownik.id }}</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Zapisz</button>
        {{ form_end(form) }}

        <p><a href="{{ path('admin_pracownicy_index') }}">← Wróć</a></p>
    </div>
{% endblock %}
